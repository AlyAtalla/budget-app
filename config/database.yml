default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>
  url: <%= ENV['DATABASE_URL'] %> # Use Railway-provided DATABASE_URL

development:
  <<: *default
  database: budget_app_development
  username: postgres
  password: 0120852868
  host: localhost

test:
  <<: *default
  database: budget_app_test
  username: postgres
  password: 0120852868
  host: localhost

production:
  <<: *default
  # In production, Rails will use DATABASE_URL from Railway
